#include <iostream> 
#include <string> 
#include <fstream> 
#include <stdio.h> 
using namespace std; 
int main()   
 
{ 
	setlocale( LC_ALL, "rus" ); 
	int p; char b[50000]; 
	char g[20]; int i, m, t; int n; 
	char str[50000]; 

	cout << "Ââåäèòå òåêñò" << endl; 
	ofstream fout("C:\\cppstudio.txt"); 
	cin.getline(str, 50000); 
	fout << str; 
	fout << ' ';
	fout.close(); 
	
	ifstream fin("\C:\\cppstudio.txt"); 
	fin.getline(b, 50000); 
	fin.close(); 
	while (true)
	{
		cout << "Ââåäèòå êîëè÷åñòâî ñëîâ, êîòîðûå áóäåòå èñêàòü" << endl;
		cin >> n; 
		if( cin.good())
		{
			cin.ignore(10, '\n');
			break;
		}
		cin.clear();
			cout << "Íåïðàâèëüíûé ââîä äàííûõ " << endl;
		cin.ignore(10, '\n');
	}
	cin.getline(g, 20); 
	string a = b; 
	for ( i = 0; i < n; i++ ) 
	{ 
		cout << "Ââåäèòå ñëîâî, êîòîðîå õîòèòå íàéòè" << endl; 
		cin.getline(g, 20); 
		p = a.find( g ); 
		if (p == -1) cout << "Òàêîãî ñëîâà íåò" << endl; 

		else 

		{ 
			cout << "Íàéäåííîå ñëîâî "; 
			m = p; 
			while ( (b[m] != ' ') ) { cout << b[m]; m++; } 
			cout << endl; 
			cout << p << endl; 
		} 
	} 
	system("pause"); 
	return 0; }
